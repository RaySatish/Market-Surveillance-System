# ---------- Python ----------
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg

# ---------- Virtual environments ----------
venv/
.venv/
env/

# ---------- IDE / Editor ----------
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# ---------- Pipeline runtime artifacts ----------
# Raw / intermediate data (regenerated by generate_trades.py & ETL)
data/
trades.csv
trades.csv.validated

# Logs (generated at runtime by the logging framework)
logs/

# Dead-letter queue (rejected rows — runtime artifact)
dead_letter/

# Pipeline checkpoints (runtime state for --resume)
.checkpoints/

# ---------- Alerts ----------
# Alert CSVs are committed so the dashboard works on GitHub/Streamlit Cloud.
# If you prefer NOT to track them, uncomment the next line:
# alerts/